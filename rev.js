let dic={"A":[3,9,0,7],"B":[6,2,4,5]}
for (var x in dic){
    let list1=[]
    a=dic[x].length
    let i=-1
    while (i<dic[x]-1(length)){
        list1.push(dic[x][a])
        a=a-1
        i++
    y=list1
    dic[x]=y
    }
}
console.log(dic)